# Log — timetap error paths round

One line per event, newest at the bottom. Format:
`## [YYYY-MM-DD HH:MM] <task id> | <what happened>`

## [2026-07-26 19:20] setup | Handoff compiled. Branch factory/error-paths created off main. Baseline verified green: 298 assertions under America/New_York, Europe/London, Australia/Sydney and UTC; `node test/lint.js` all clear. 13 tasks pending.

## [2026-07-27 00:00] orientation | Read HANDOFF.md in full. Baseline re-verified independently: `node test/tests.js` 298 passed / 0 failed, `node test/lint.js` all clear. Six questions asked; human answered "yes to all". Answers recorded in the HANDOFF Orientation Q&A block — Playwright pinned exact as the dev-only browser, npm install approved, deploy.sh only ever run with a stub clasp prepended to PATH, B1 captures category at quarantine time (wire payload unchanged), C2 gets a committed golden fixture, drawer wording is the agent's to choose within the no-judgment law. No build work started.

## [2026-07-27 00:40] A1 | DONE. `test/harness.js` gains `setServerReject(msg)`, which wraps the real `applyOp_` so it throws — the rejection then travels the real `applyOps` bookkeeping and comes back through the *success* handler as a non-empty `errors` array, the one path that counts against an op's try count. `reset()` clears it. 21 assertions added to `test/tests.js` as sections 34/34b/34c/34d. 298 → 319, green under America/New_York, Europe/London, Australia/Sydney, UTC; `node test/lint.js` clear. **All five criteria pass against unmodified `Index.html` — no finding, nothing parked.**

## [2026-07-27 00:40] A1 | CHECKER. Mutation-tested in a throwaway copy under scratchpad (never in the repo). Neutering the reject switch fails 12 assertions; a cap that keeps the oldest 50 instead of the newest fails the ordering assertion. **The first pass of this found a real flaw in my own test:** moving the give-up threshold from 5 to 6 still passed 319/319, because the second pump waited for *any* dead entry instead of stopping at the fifth attempt — the vacuous-assertion class `test/README.md:63` records. Fixed by pumping to exactly 5 attempts and asserting there. Now off-by-one in either direction fails: threshold 6 → 4 failures, threshold 4 → 3 failures. Control copy 319/319.

## [2026-07-27 01:20] B1 | Criteria 1-4 done. Ops that know their category or start leave it in a ref-keyed localStorage index (`tt.blocks.v1`) at enqueue time; `quarantine()` reads it and stores `key`/`startMs` on the dead entry. Wire payload unchanged, per the orientation decision — the server sees exactly the op it saw before. Added `deadRows()`, which normalises entries for a reader and names missing parts rather than rendering blank. 18 assertions (319 → 337), green under 4 zones, lint clear. CHECKER: three mutations all caught — index forgetting the category (1 failure), `endMs` mistaken for a block start (1), quarantine storing nothing new (7). Criterion 5 needs a rendering surface and is carried into B2.

## [2026-07-27 01:20] B1 | FINDING F1: the boot-time set-aside banner erases itself. `boot()` shows the count, then `loadServerState()` succeeds and calls `hideErr()` — so the message survives only when the state load ALSO fails, i.e. it is visible exactly when it is least useful. Contract item 9 requires it visible. FINDING F2: it reads "1 write were set aside". Both recorded with criteria in progress.md before any fix; both fixes land in B2.

## [2026-07-27 02:10] B2 | DONE. Third overlay sheet `#sheetDead`, built like `#sheetSplit`/`#sheetSit`. `#err` becomes a door when the dead list is non-empty — role=button, tabindex=0, aria-label, Enter/Space as well as tap — and goes inert when it is empty. One row per set-aside write, newest first: clock time · category, "tried to save the mark, block started 10:00 AM", then the server's own reason. Op types never reach the screen; an op type with no wording falls back to the raw type rather than a blank row. Fixes F1 (hideErr now falls back to the standing set-aside notice instead of erasing it) and F2 (verb agrees with the count). 35 assertions (337 → 372), 4 zones, lint clear.

## [2026-07-27 02:10] B2 | CHECKER. 7 mutations: hideErr erasing the notice (2 failures), oldest-first rows (5), raw op types on screen (5), blank instead of "unknown" (1), openDead never showing the sheet (crash), banner not announced as a button (1). **P2 initially passed under mutation** — the verb-agreement fix was unverified because every test I had written used the plural. Added section 36h for the singular case; the mutation now fails as it should. B1 criterion 5 verified here in section 36f.

## [2026-07-27 02:40] B3 | DONE. Each row gets a DISCARD button. Rows are identified by a token built from the entry's timestamp and its op's id/type/ref, not by position — discarding renumbers everything after it, so a second tap on an already-gone row would otherwise land on whatever slid into its place. Discard touches the dead list only: the queue is never read or written. Last entry out closes the drawer and hides the banner, and the boot message does not come back on reload. 19 assertions (372 → 391), 4 zones, lint clear. CHECKER: 3 mutations all caught — position-based identity (3 failures), discard clearing the queue (1), drawer left open on the last discard (1). **Stage B complete.**

## [2026-07-27 05:55] C1 | DONE. `dailyRollup` is now a wrapper around `rollupOnce_` (the old body, unchanged) that records the outcome in script property `ROLLUP_LAST` and rethrows. The record keeps `outcome`, `atMs`, `lastSuccessMs`, `lastFailureMs`, `lastFailureWhy`, so a failure never erases the evidence of the last good run — knowing the numbers went stale eleven days ago is the point. Recording is wrapped in its own try/catch so it can never become the thing that breaks the rollup. 19 assertions (391 → 410), 4 zones, lint clear. CHECKER: 3 mutations all caught — swallowing the error instead of rethrowing (5 failures), the record starting from scratch each run (3), failures recording nothing (6).

## [2026-07-27 06:20] C2 | DONE. `stampGrid_` puts "last rebuilt <yyyy-MM-dd HH:mm> <tz>" in row 1, in the first column past the last data column — not a row above the data, which would silently shift every row reference a reader has already pointed at these tabs. Written as part of the wholesale grid rebuild, so there is exactly one stamp per tab however many times the rollup runs, and a tab's stamp can never be newer than that tab's own numbers. Golden fixture captured per timezone from commit d048ca3 (pre-stamp) and committed at `test/fixtures/rollup-golden.json`; the test asserts every pre-existing cell is byte-identical and that the single new column holds only the stamp. 30 assertions (410 → 440), 4 zones, lint clear.

## [2026-07-27 06:20] C2 | Two notes. (1) The stamp first read the real wall clock: `new Date()` is the only bare one in `Code.gs` — the other nine reads go through `Date.now()` — so it ignored the virtual clock and a 120-minute wait left it unchanged. Now `new Date(Date.now())`, identical in production and consistent with the rest of the file. (2) Existing test 29 ("no column invented from a clock time or a subject line") scans the header row, and the stamp lives there. Excluded it **by name** rather than loosening the filter, and added an assertion pinning that exactly one such cell exists — a column genuinely invented from a calendar title still fails that test.

## [2026-07-27 06:20] C2 | CHECKER. 5 mutations: stamp row pushed above the data (6+ failures, the shift the task exists to prevent), weekly tab unstamped (4), stamp hardcoded to UTC (1), stamp patched in after the grid write (1). A fifth mutation writing a stamp before the work succeeded was NOT caught — investigated rather than papered over: the stamp is written inside the same wholesale grid write as the numbers, so per tab the two are always the same age. Added section 39e to assert that structurally (weekly fails mid-run: old numbers and old stamp together; daily: new numbers and new stamp together).

## [2026-07-27 06:45] C3 | DONE. `rollupStatus()` reports the recorded outcome through `say_` — last run and when, last success, last failure with its reason — answering from the script property, so it still works in the case that produces most stale stamps: the spreadsheet cannot be opened at all. Nothing on record says so plainly; an unreadable record says that and names the property to clear; neither throws and neither prints `undefined`. Extracted `stampTime_` so C2's stamp and this report say a moment the same way. 19 assertions (440 → 459), 4 zones. CHECKER: 4 mutations all caught — unreadable record throwing (3 failures + crash), report not reaching the log (1), last success omitted (1), "never run" printing undefined (2).

## [2026-07-27 06:45] C3 | The existing ASCII lint rule caught an em dash I put in a `Code.gs` string literal ("marks in Code.gs are ASCII only" — `parseTitle_` matches `-` literally and a lookalike silently stops parsing). Replaced with a comma. The rule did exactly the job it was written for. **Stage C complete: 298 → 459 assertions, 4 zones, lint clear.**

## [2026-07-27 07:10] D1 | DONE. First `package.json` — `playwright` pinned to exactly 1.62.0 under devDependencies, no caret, so the tier-4 canary means something. Playwright 1.62.0 wants chromium-1234 and that is already in the machine's cache, so no browser download was needed. `test/serve.js` emits what the real `doGet` serves (html + the meta tags it injects + title + configured keys); it runs as its own process because `harness.js` replaces setTimeout/setInterval with a virtual clock and a browser driver sharing that process would wait forever. `test/headless.js` renders that page at a phone viewport and reports a fact read back out of the live DOM: 6 category cells, matching the 6 configured categories. Three new lint rules: source files pull nothing in, browser is a devDependency, version pinned exactly.

## [2026-07-27 07:10] D1 | CHECKER. Lint mutations all caught: browser moved to `dependencies`, a caret pin, a CDN `<script src>` in Index.html, a `require()` in Code.gs. Both missing-browser paths exercised for real — no `node_modules` exits 1 naming `npm install`; an empty `PLAYWRIGHT_BROWSERS_PATH` exits 1 naming `npx playwright install chromium`. Everything present exits 0. **A skipped run is never reported as a pass.** TIER-4 CANARY: chromium-1234 launches on this machine.

## [2026-07-27 07:50] D2 | 5 of 6 criteria met. The runner builds the document the way Apps Script does — a fragment wrapped in its own head/body with the meta tags injected, no charset meta (that would be a tag doGet never sends). It loads `test/smoke.js` into the page and reads its result; the checks are not reimplemented. Phone viewport 390x844 with isMobile, 17 passed / 0 failed / 2 skipped = 19, matching the 19 `ok(` sites in the file. Verified error paths: a failing check exits 1 and names it; `pass: 0` exits 1; no checks in the file at all exits 1.

## [2026-07-27 07:50] D2 | CHECKER found two design faults in my own first attempt. (1) The runner injected doGet's own meta list and then compared the DOM against it — self-fulfilling, and it left D4 with nothing to compare, since the harness had no list of its own. A fourth tag added to `doGet` passed happily. The harness now declares `META_TAGS` in full, injects that, and cross-checks it against what doGet sends; both drift cases now exit 1 naming which side is missing what. (2) A check deleted from `smoke.js` still passes, because the expected count is derived from the same file — the derived count catches a check that exists but does not run, not one that was removed. Recorded rather than papered over; git review is the guard against deletion.

## [2026-07-27 07:50] D2 | FINDING F3 (fixed): `smoke.js:78` counted `ok('no block running, ring not checked', true, ...)` — literally `true`, a pass whatever the page did, and counted in `pass`. Now reported as two named skips. `pass` counts only checks that could have failed, and `pass + fail + skipped` equals the number of `ok(` sites, so a check added inside an unreached branch cannot silently go unrun. FINDING F4 (PARKED, escalated): no check in `smoke.js` is viewport-sensitive — with no meta tags at all the page lays out at 980px and all 17 still pass — and Blink re-lays-out on a dynamically appended viewport meta, so "after" is indistinguishable from "before". D2's sixth criterion cannot be demonstrated as written. Built the layout-width control instead and left the criterion unmet. Direct evidence that the phone paste is not superseded.

## [2026-07-27 08:05] D3 | DONE. Second viewport added (desktop 1280x800, isMobile false) alongside the phone (390x844, isMobile). Each is labelled with its dimensions and every problem is prefixed with the viewport it came from. Both green: 17 passed / 0 failed / 2 skipped each. CHECKER, both error criteria exercised against fixture copies in scratchpad and never against the repo's own files: a `@media (max-width: 500px)` rule narrowing one cell fails "every cell is the same width (40 / 184)" at the phone only, passes desktop, exits 1 and names phone; a `smoke.js` fixture returning `pass: 0` below 500px exits 1 naming phone while desktop reports its full 17. First attempt at the CSS fixture matched nothing — the grid cells are divs with `role="button"`, not `<button>` elements, because they contain the note `<input>` a button would strip.

## [2026-07-27 08:25] D4 | DONE (HIGH RISK task). Four rules added to `test/lint.js`: both lists parsed non-empty, neither declares a name twice, `doGet` and `test/headless.js` inject the same tags, and every name is one of the four Apps Script permits. The first two exist because the last two compare two lists — a parser that quietly matched nothing would make both comparisons pass while comparing nothing, which is the bug `test/README.md:63` records. Fixture cases run against copies in scratchpad; `Code.gs` is never edited in place.

## [2026-07-27 08:25] D4 | Divergence cases, rule intact — 1. lists agree -> pass. 2. fourth tag in doGet only -> FAIL, "google-site-verification — doGet sends it, test/headless.js does not inject it". 3. tag in harness only -> FAIL, names it and says which side. 4. same tag different content -> FAIL, names viewport and prints both strings. 5. a name Apps Script does not permit (theme-color) -> FAIL. 6. same tags in a different order -> pass, order is not meaningful. Extra guards: 7. harness list renamed so the parser finds nothing -> FAIL "no META_TAGS entries parsed". 8. doGet declares viewport twice -> FAIL "neither list declares the same meta tag twice".

## [2026-07-27 08:25] D4 | **META-TEST PERFORMED** (required by the criteria, not skipped). Replaced the drift rule with `check('...', [], ...)` — one that always passes — and re-ran all six cases: cases 2, 3 and 4 flipped from fail to pass, i.e. three criteria stopped detecting. The criterion asks for at least three. Case 5 still failed (it is a separate rule, correctly unaffected) and cases 1 and 6 still passed. The rule's tests are therefore capable of failing, and the rule is load-bearing rather than decorative.

## [2026-07-27 08:45] D5 | DONE. `deploy.sh` runs the headless render as a third gate after lint and the timezone suite. A missing browser stops the deploy instead of being waved through. Header comment now describes all three layers, which is what `--help` prints. Exercised in a fixture copy with a stub `clasp` first on PATH and a dummy `.clasp.json`, per the orientation decision — the real `.clasp.json` was never read and nothing was ever pushed. 1. all three pass -> exit 0, reaches push, stub log shows `deployments`, `push --force`, `deploy -i`. 2. a failing smoke check -> exit 1, "headless checks failed. Nothing deployed.", **stub log empty — clasp never invoked**. 3. browser missing -> exit 1, prints `npx playwright install chromium`, stub log empty. 4. `--no-test` -> all three skipped, deploy proceeds exactly as before. 5. `--help` -> lists the three layers by name and file. **Stage D complete.**

## [2026-07-27 09:00] E1 | DONE. `README.md` no longer opens with "no dependencies" — it says the four source files import nothing and need no build step, and that the dev-only tooling has one dependency that is never deployed. `test/README.md`: the assertion count corrected 253 → 459 (verified against what the suite actually reports), a new section for `node test/headless.js` with the install command, the six new lint rules added to the existing table with the bug each prevents, and `smoke.js`'s return shape updated to `{ pass, fail, failed, skipped }`. The phone-paste instruction is still there and now says explicitly why the headless run does not supersede it — with the concrete reason from F4: in a headless engine `dvh` and `vh` are equal and every check is relative, so at 980px with no viewport meta all of them still pass. Every command the docs name was executed: all exit 0. **Stage E complete.**

## [2026-07-27 09:15] final | FINDING F5 (reported, not actioned): Contract item 6 says `appsscript.json` asks for exactly one OAuth scope. It asks for three, and did so at the branch point — the file is byte-identical to `main` and was never touched on this branch. All three are load-bearing (calendar; spreadsheets for the rollup; script.scriptapp for the nightly trigger). Deleting two to satisfy the sentence would break the rollup and the trigger, so the finding is reported instead. The same stale claim appears in `README.md` and in the handoff's own orientation table.

## [2026-07-27 09:15] final | SIGN-OFF. Full suite green TWICE in a row under all four timezones: 459 passed / 0 failed each time. `node test/lint.js` all clear both runs (all 8 pre-existing rules verified still present by name, plus 7 added this round). `node test/headless.js` green both runs, both viewports. Contract checked item by item: 1-5 pass; 6 is F5; 7-13 covered by sections 34-37e; 14-18 by 38-40e; 19-23 by the headless runner, the drift lint and the deploy.sh gate. 13 commits on factory/error-paths, nothing pushed, main untouched. Stages A-E all complete, nothing parked, 2 findings escalated.

## [2026-07-27 07:00] FIX PASS | Human ruled on all three decisions from factory/REVIEW.md: (1) D2 criterion 6 retired, replaced by the layout-width control; (2) contract item 6 corrected to three OAuth scopes, manifest untouched; (3) rollup invariant is per-tab honesty, plus a failed write must never blank a tab. Recorded as amendments A1-A3 under "Contract amendments" in HANDOFF.md, with items 6 and 16 rewritten in place and assertions 24-27 added. HANDOFF.md edited by human decision — the second sanctioned exception to "never edit this file", and the only one besides the Orientation Q&A.

## [2026-07-27 07:00] FIX PASS | F-1 (BLOCKING) fixed. DISCARD now arms on the first tap and acts on the second, reusing the app's own arm/confirm idiom (`.arming`, `CFG.confirmTimeoutMs`) rather than inventing a guard. This is structural, not timing-based: a row that slides up into the vacated space is not armed, so a tap never aimed at it cannot discard it, however fast the taps are. Also stopped redrawing the whole list on discard (`dropRow` removes one node) and cleared the host in `closeDead`. New tier-1 sections 37f/37g/37h/37i pin the arming behaviour; 37e was rewritten to test the stale-token path, which the arming change did not remove. **Existing 37/37b/37d were changed to a two-tap gesture — every assertion in them is byte-identical, only the gesture reaching it changed.** VACUITY CHECK: reverting `armDead` to `discardDead` in a fixture copy makes 37f fail 3 assertions and the headless drawer phase exit 1 naming "discarded 2 writes".

## [2026-07-27 07:00] FIX PASS | F-1 tier-3 twin added to `test/headless.js`: opens the drawer in Chromium at 390x844 and double-taps DISCARD at one fixed coordinate 120ms apart, asserting exactly one entry left and that it was the one aimed at. Needs a real origin (localStorage on setContent's about:blank is a different store), so it routes `http://timetap.invalid/` and stubs `google.script.run` with an always-succeeding server; the meta and smoke checks keep using setContent, and the comment says why they differ. This is the check the DOM shim structurally cannot host — no layout means no control moving under a finger.

## [2026-07-27 07:00] FIX PASS | F-2 fixed two ways, per amendment A3. (1) Both grids are built before either is written, so a failure while building leaves both tabs untouched. (2) `writeGrid_` now writes then trims instead of clearing first — a write that throws used to leave the tab with no numbers AND no stamp, which is worse than stale because nothing in the spreadsheet said anything was wrong. §39e's stub was changed to break the weekly tab's `getRange` rather than `writeGrid_` itself, so the blanking path is actually reachable from the suite; stubbing the function meant its own `clear()` never ran, which is why the loop missed this. New §39f (build failure touches neither tab) and §39g (a smaller later grid leaves none of the bigger one behind, which write-then-trim needs to guarantee). Golden fixture NOT regenerated and still byte-identical. Harness FSheet now models an allocated 1000x26 sheet with `getMaxRows`/`getMaxColumns`/`clearContent` and an overlaying `setValues`, because a shim that replaced the whole array could not tell clear-then-write from write-then-trim. VACUITY CHECK: restoring clear-first fails 3 assertions in §39e; restoring build-as-you-write fails §39f.

## [2026-07-27 07:00] FIX PASS | F-3 fixed: the raw NUL byte in `test/headless.js:257` is now the `'\0'` escape, so git treats the file as text again and its diffs are reviewable. New lint rule over every source and test text file. VACUITY CHECK: reintroducing the byte in a fixture copy makes lint exit 1 naming the file.

## [2026-07-27 07:00] FIX PASS | F-5 fixed: README's manifest row said "One OAuth scope" against a manifest asking for three. New lint rule parses every scope count stated in README.md and SETUP.md and compares it to `appsscript.json`, so the claim cannot go stale silently again — it caught the stale line by name before it was fixed. `appsscript.json` untouched and still byte-identical to `main`.

## [2026-07-27 07:00] FIX PASS | F-4 fixed: progress.md's RUN SUMMARY said "All 13 tasks done, none parked" three lines above D2's own row saying criterion 6 was parked. Status line corrected to "12 of 13 done; D2 parked on one criterion", D2's table row updated, and F4's evidence left intact. F-6 (cosmetic) done too: a write set aside while the drawer is open now appears without reopening (§37j), and closing the drawer leaves no rows behind in it (§37k).

## [2026-07-27 07:00] FIX PASS | VERIFIED. 459 → 492 assertions, green twice in a row and under America/New_York, Europe/London, Australia/Sydney, UTC. `node test/lint.js` all clear, 17 rules. `node test/headless.js` green at both viewports plus the drawer phase. `deploy.sh` re-exercised in a fixture copy with a stub `clasp` first on PATH and a dummy `.clasp.json` — reached the push step, stub log shows `deployments`/`push --force`/`deploy -i AKfycbSTUB`, so the real binary was never invoked and nothing was pushed. Golden rollup fixture untouched. Armed state visually confirmed at 390x844: amber ring on the armed row only, no horizontal overflow.

## [2026-07-27 10:10] F2-1 | Comment fixed and the arm/confirm property pinned, per amendment A4. `Index.html`'s `discardDead` docblock claimed "the list is NOT redrawn… Nothing may move while a finger is down" — the first half is true, the second is not: removing one row from a flex column slides every row below it up by exactly the amount redrawing did. Rewritten to say what actually holds (rows do move; the row that slides under the finger arrives unarmed, so destroying it costs a further deliberate tap against a button already reading TAP AGAIN TO DISCARD) and to point at A4 and the new check. `dropRow`'s one-line comment carried the same false claim ("leaving every other row where it was") and was corrected too — the two comments now agree with each other and with `Index.html:738-742`. No behaviour change: `git diff Index.html` is comments only.

## [2026-07-27 10:10] F2-1 | `factory/review-repro-2/tap-count.js` folded into `test/headless.js` as a `checkTapCount` phase and deleted from `factory/`. It seeds 5 set-aside writes, opens the drawer, and taps one fixed coordinate 2, 3, 4 and 6 times, recording via `elementFromPoint` what the button under the finger said *before* each tap. Measured: 1, 1, 2 and 3 entries destroyed — the table in A4 exactly. Asserts three things: the count matches floor(taps/2); no tap that destroyed an entry found anything other than `TAP AGAIN TO DISCARD` under the finger; the page throws nothing. Shares the drawer phase's real-origin setup via a new `openDrawerWith` helper (localStorage on setContent's about:blank is a different store). Check count per viewport unchanged at 19 — this is a phase, not a smoke check — so `test/README.md` needed no count edit, but gained a paragraph describing the phase and how to make it fail.

## [2026-07-27 10:10] F2-1 | VACUITY CHECK: in a fixture copy with `armDead` reverted to discard on the first tap, `node test/headless.js` exits 1 and the tap-count phase names every offending tap — "tap 1 destroyed 1 while the button said \"DISCARD\"" through tap 5, at all four tap counts, plus the count mismatches (2 taps destroyed 2 not 1; 6 taps destroyed 5 not 3). The existing drawer phase fires alongside it. Tier-1 §37f–§37k unchanged and still passing; suite still 492/0.

## [2026-07-27 10:35] F2-2 | The NUL-byte rule now covers what assertion 26 says it covers. The old rule listed seven root files plus a non-recursive read of `test/` — 14 files. `factory/`, `site/`, `.github/`, `test/fixtures/`, `.gitignore`, `.claspignore` and `package-lock.json` were all outside it, and review 2 proved it by appending a NUL to `factory/log.md` and watching lint print "all clear" and exit 0. The census is now the repo's own: `git ls-files -z` when there is a `.git`, and a recursive walk skipping `.git`, `node_modules` and `.clasp.json` when there is not — an export or a fixture copy still has to be lintable. Both modes see the same 32 files today, and the rule prints which mode it used and how many files it read, so a rule that quietly scanned nothing cannot print the same "ok" as one that scanned the repo.

## [2026-07-27 10:35] F2-2 | Binary files get a named exemption, never an extension filter. `BINARY_EXEMPT` is empty on purpose — there are none tracked today — and a file listed there is reported as a skip with its reason rather than passing silently. A file that cannot be read at all is a failure naming the file and the errno, not a silent continue. The one thing that is skipped without comment is a path tracked but deleted from the worktree, which is not a file anyone can review either way.

## [2026-07-27 10:35] F2-2 | VERIFIED, all four criteria, in fixture copies under the scratchpad — the repo was never dirtied. (1) NUL appended to `factory/log.md`: lint exits 1 naming it. (2) NUL appended to `site/index.html`, `.github/workflows/pages.yml` and `test/fixtures/rollup-golden.json`: lint exits 1 naming all three. (3) Repo as it stands: 32 files, all clear, exit 0 — the rule does not start failing on files that are fine. (4) `chmod 000` on a tracked file: lint exits 1 with "site/build.mjs — could not be read at all: EACCES"; and a tracked binary added to `BINARY_EXEMPT` prints "skipped site/logo.png — exempt: declared binary in test/lint.js" and exits 0. The no-git fallback was exercised separately: same 32 files, "no git directory — walked the tree", and the `factory/log.md` NUL still caught. `test/README.md`'s rule table row renamed to match. Suite still 492/0.

## [2026-07-27 11:00] F2-3 | Two live docs corrected and the rule widened to match assertion 27. `factory/BRIEF.md:71` said "Manifest, one OAuth scope" in its orientation table — stated as a current fact about the repo, which is exactly the route by which the wrong number got quoted into a build contract — and `factory/PLAN.md:33` said "still asks for exactly one OAuth scope". Both now say three, PLAN with a pointer to amendment A2 so the correction is on the record rather than silent. `appsscript.json` untouched and still byte-identical to `main` (`git diff main..HEAD -- appsscript.json` empty); all three scopes load-bearing per A2.

## [2026-07-27 11:00] F2-3 | The rule now reads every `.md` in the repo instead of `README.md` and `SETUP.md`, sharing F2-2's census so a doc dropped into any directory is covered without editing the rule. Eight files are exempt by name, each with its reason: REVIEW.md, REVIEW-2.md, FIXES.md, FIXES-2.md, log.md, progress.md and STATE.md quote the wrong sentence deliberately — they are the record of what was wrong and when, and correcting them would destroy the evidence. HANDOFF.md is the eighth and the weakest entry: amendment A2 quotes the original contract sentence verbatim and the file is under a never-edit guardrail, so a wrong count introduced elsewhere in it would not be caught. Named in the comment rather than left to be discovered. `test/README.md:53` described the bug using the wrong number in prose and was reworded to state no count at all, rather than being added to the exemption list — a live doc earns its way past this rule by being right, not by being excused.

## [2026-07-27 11:00] F2-3 | VERIFIED, all five criteria, in a fixture copy. (1) Repo as it stands: all clear, 6 of 14 .md checked, 8 exempt. (2) BRIEF reverted to "one OAuth scope": exits 1 naming `factory/BRIEF.md:71`. (3) PLAN reverted: exits 1 naming `factory/PLAN.md:33`. (4) EXEMPTION VACUITY — emptying `SCOPE_QUOTE_EXEMPT` makes lint exit 1 on the historical files (FIXES-2, FIXES, HANDOFF, REVIEW-2 and more), so the exemption is load-bearing and tested rather than assumed. (5) A new `docs-new.md` at the root and a new `site/NOTES.md` — a directory the old rule never looked at — both caught by name and line, neither `git add`ed, so an untracked doc gets no free pass.

## [2026-07-27 11:00] F2-3 | FOUND AND FIXED while testing F2-3, in F2-2's census: `.gitignore` says `node_modules/`, which matches a directory and not a symlink pointing at one, so a checkout that symlinks its node_modules had the whole tree offered to the NUL rule, which then failed with "EISDIR: illegal operation on a directory". The fixture copies used in this pass symlink node_modules, which is how it surfaced. The rule now skips anything that is not a regular file and says so out loud — nothing that is not a regular file can hold a NUL of its own, since git stores a symlink as its target path. The real repo never hit this; a developer's checkout would have.

## [2026-07-27 11:35] F2-4 | §39d crashed in any timezone outside the contracted four instead of skipping. It reported "FAIL there is a golden for this timezone" and then dereferenced the missing golden on the very next line — `TypeError: Cannot read properties of undefined (reading 'days')` — aborting the run, so §40 and §40e never executed and the exit was a stack trace rather than a result. Contract item 2 names four zones, so a fifth zone is not a contract violation; it is a developer getting a crash instead of a reason. The harness gains `skip(label, why)` mirroring `smoke.js`'s idiom — named, with the reason, counted nowhere near `pass` — and §39d's body is now guarded by whether a golden for the current zone exists.

## [2026-07-27 11:35] F2-4 | Deliberately NOT a silent pass. The old `chk('there is a golden for this timezone')` could not stay: inside the guarded branch it can only ever pass, which is the counted-assertion-that-cannot-fail class `test/README.md:63` records. It is replaced by a check that can fail — the golden for this zone holds a non-empty grid for both tabs — which keeps the count at 492 in the four contracted zones and bites on a fixture whose grids were emptied (verified). The summary line prints ", 1 skipped" and lists the skip with its reason, so a run with a skip is visibly different from a run without one.

## [2026-07-27 11:35] F2-4 | VERIFIED. `TZ=Asia/Kolkata` and `TZ=Pacific/Chatham` (a 45-minute offset) both run to completion, exit 0, and report "482 passed, 0 failed, 1 skipped" with the skip naming the zone and listing the four the fixture holds — 482 is 492 minus §39d's ten assertions, so nothing else was lost and everything after §39d ran. All four contracted zones still 492/0, count unchanged. Error path, in a fixture copy: fixture deleted, fixture replaced with non-JSON, and fixture parsed-but-holding-no-byZone all print a plain sentence naming the file with the `git checkout` command to restore it, and exit 1 — no TypeError, no require stack noise (the MODULE_NOT_FOUND message is truncated to its first line for that reason). The golden fixture itself is untouched and byte-identical.
