# Log — timetap error paths round

One line per event, newest at the bottom. Format:
`## [YYYY-MM-DD HH:MM] <task id> | <what happened>`

## [2026-07-26 19:20] setup | Handoff compiled. Branch factory/error-paths created off main. Baseline verified green: 298 assertions under America/New_York, Europe/London, Australia/Sydney and UTC; `node test/lint.js` all clear. 13 tasks pending.

## [2026-07-27 00:00] orientation | Read HANDOFF.md in full. Baseline re-verified independently: `node test/tests.js` 298 passed / 0 failed, `node test/lint.js` all clear. Six questions asked; human answered "yes to all". Answers recorded in the HANDOFF Orientation Q&A block — Playwright pinned exact as the dev-only browser, npm install approved, deploy.sh only ever run with a stub clasp prepended to PATH, B1 captures category at quarantine time (wire payload unchanged), C2 gets a committed golden fixture, drawer wording is the agent's to choose within the no-judgment law. No build work started.

## [2026-07-27 00:40] A1 | DONE. `test/harness.js` gains `setServerReject(msg)`, which wraps the real `applyOp_` so it throws — the rejection then travels the real `applyOps` bookkeeping and comes back through the *success* handler as a non-empty `errors` array, the one path that counts against an op's try count. `reset()` clears it. 21 assertions added to `test/tests.js` as sections 34/34b/34c/34d. 298 → 319, green under America/New_York, Europe/London, Australia/Sydney, UTC; `node test/lint.js` clear. **All five criteria pass against unmodified `Index.html` — no finding, nothing parked.**

## [2026-07-27 00:40] A1 | CHECKER. Mutation-tested in a throwaway copy under scratchpad (never in the repo). Neutering the reject switch fails 12 assertions; a cap that keeps the oldest 50 instead of the newest fails the ordering assertion. **The first pass of this found a real flaw in my own test:** moving the give-up threshold from 5 to 6 still passed 319/319, because the second pump waited for *any* dead entry instead of stopping at the fifth attempt — the vacuous-assertion class `test/README.md:63` records. Fixed by pumping to exactly 5 attempts and asserting there. Now off-by-one in either direction fails: threshold 6 → 4 failures, threshold 4 → 3 failures. Control copy 319/319.

## [2026-07-27 01:20] B1 | Criteria 1-4 done. Ops that know their category or start leave it in a ref-keyed localStorage index (`tt.blocks.v1`) at enqueue time; `quarantine()` reads it and stores `key`/`startMs` on the dead entry. Wire payload unchanged, per the orientation decision — the server sees exactly the op it saw before. Added `deadRows()`, which normalises entries for a reader and names missing parts rather than rendering blank. 18 assertions (319 → 337), green under 4 zones, lint clear. CHECKER: three mutations all caught — index forgetting the category (1 failure), `endMs` mistaken for a block start (1), quarantine storing nothing new (7). Criterion 5 needs a rendering surface and is carried into B2.

## [2026-07-27 01:20] B1 | FINDING F1: the boot-time set-aside banner erases itself. `boot()` shows the count, then `loadServerState()` succeeds and calls `hideErr()` — so the message survives only when the state load ALSO fails, i.e. it is visible exactly when it is least useful. Contract item 9 requires it visible. FINDING F2: it reads "1 write were set aside". Both recorded with criteria in progress.md before any fix; both fixes land in B2.

## [2026-07-27 02:10] B2 | DONE. Third overlay sheet `#sheetDead`, built like `#sheetSplit`/`#sheetSit`. `#err` becomes a door when the dead list is non-empty — role=button, tabindex=0, aria-label, Enter/Space as well as tap — and goes inert when it is empty. One row per set-aside write, newest first: clock time · category, "tried to save the mark, block started 10:00 AM", then the server's own reason. Op types never reach the screen; an op type with no wording falls back to the raw type rather than a blank row. Fixes F1 (hideErr now falls back to the standing set-aside notice instead of erasing it) and F2 (verb agrees with the count). 35 assertions (337 → 372), 4 zones, lint clear.

## [2026-07-27 02:10] B2 | CHECKER. 7 mutations: hideErr erasing the notice (2 failures), oldest-first rows (5), raw op types on screen (5), blank instead of "unknown" (1), openDead never showing the sheet (crash), banner not announced as a button (1). **P2 initially passed under mutation** — the verb-agreement fix was unverified because every test I had written used the plural. Added section 36h for the singular case; the mutation now fails as it should. B1 criterion 5 verified here in section 36f.

## [2026-07-27 02:40] B3 | DONE. Each row gets a DISCARD button. Rows are identified by a token built from the entry's timestamp and its op's id/type/ref, not by position — discarding renumbers everything after it, so a second tap on an already-gone row would otherwise land on whatever slid into its place. Discard touches the dead list only: the queue is never read or written. Last entry out closes the drawer and hides the banner, and the boot message does not come back on reload. 19 assertions (372 → 391), 4 zones, lint clear. CHECKER: 3 mutations all caught — position-based identity (3 failures), discard clearing the queue (1), drawer left open on the last discard (1). **Stage B complete.**

## [2026-07-27 05:55] C1 | DONE. `dailyRollup` is now a wrapper around `rollupOnce_` (the old body, unchanged) that records the outcome in script property `ROLLUP_LAST` and rethrows. The record keeps `outcome`, `atMs`, `lastSuccessMs`, `lastFailureMs`, `lastFailureWhy`, so a failure never erases the evidence of the last good run — knowing the numbers went stale eleven days ago is the point. Recording is wrapped in its own try/catch so it can never become the thing that breaks the rollup. 19 assertions (391 → 410), 4 zones, lint clear. CHECKER: 3 mutations all caught — swallowing the error instead of rethrowing (5 failures), the record starting from scratch each run (3), failures recording nothing (6).

## [2026-07-27 06:20] C2 | DONE. `stampGrid_` puts "last rebuilt <yyyy-MM-dd HH:mm> <tz>" in row 1, in the first column past the last data column — not a row above the data, which would silently shift every row reference a reader has already pointed at these tabs. Written as part of the wholesale grid rebuild, so there is exactly one stamp per tab however many times the rollup runs, and a tab's stamp can never be newer than that tab's own numbers. Golden fixture captured per timezone from commit d048ca3 (pre-stamp) and committed at `test/fixtures/rollup-golden.json`; the test asserts every pre-existing cell is byte-identical and that the single new column holds only the stamp. 30 assertions (410 → 440), 4 zones, lint clear.

## [2026-07-27 06:20] C2 | Two notes. (1) The stamp first read the real wall clock: `new Date()` is the only bare one in `Code.gs` — the other nine reads go through `Date.now()` — so it ignored the virtual clock and a 120-minute wait left it unchanged. Now `new Date(Date.now())`, identical in production and consistent with the rest of the file. (2) Existing test 29 ("no column invented from a clock time or a subject line") scans the header row, and the stamp lives there. Excluded it **by name** rather than loosening the filter, and added an assertion pinning that exactly one such cell exists — a column genuinely invented from a calendar title still fails that test.

## [2026-07-27 06:20] C2 | CHECKER. 5 mutations: stamp row pushed above the data (6+ failures, the shift the task exists to prevent), weekly tab unstamped (4), stamp hardcoded to UTC (1), stamp patched in after the grid write (1). A fifth mutation writing a stamp before the work succeeded was NOT caught — investigated rather than papered over: the stamp is written inside the same wholesale grid write as the numbers, so per tab the two are always the same age. Added section 39e to assert that structurally (weekly fails mid-run: old numbers and old stamp together; daily: new numbers and new stamp together).

## [2026-07-27 06:45] C3 | DONE. `rollupStatus()` reports the recorded outcome through `say_` — last run and when, last success, last failure with its reason — answering from the script property, so it still works in the case that produces most stale stamps: the spreadsheet cannot be opened at all. Nothing on record says so plainly; an unreadable record says that and names the property to clear; neither throws and neither prints `undefined`. Extracted `stampTime_` so C2's stamp and this report say a moment the same way. 19 assertions (440 → 459), 4 zones. CHECKER: 4 mutations all caught — unreadable record throwing (3 failures + crash), report not reaching the log (1), last success omitted (1), "never run" printing undefined (2).

## [2026-07-27 06:45] C3 | The existing ASCII lint rule caught an em dash I put in a `Code.gs` string literal ("marks in Code.gs are ASCII only" — `parseTitle_` matches `-` literally and a lookalike silently stops parsing). Replaced with a comma. The rule did exactly the job it was written for. **Stage C complete: 298 → 459 assertions, 4 zones, lint clear.**

## [2026-07-27 07:10] D1 | DONE. First `package.json` — `playwright` pinned to exactly 1.62.0 under devDependencies, no caret, so the tier-4 canary means something. Playwright 1.62.0 wants chromium-1234 and that is already in the machine's cache, so no browser download was needed. `test/serve.js` emits what the real `doGet` serves (html + the meta tags it injects + title + configured keys); it runs as its own process because `harness.js` replaces setTimeout/setInterval with a virtual clock and a browser driver sharing that process would wait forever. `test/headless.js` renders that page at a phone viewport and reports a fact read back out of the live DOM: 6 category cells, matching the 6 configured categories. Three new lint rules: source files pull nothing in, browser is a devDependency, version pinned exactly.

## [2026-07-27 07:10] D1 | CHECKER. Lint mutations all caught: browser moved to `dependencies`, a caret pin, a CDN `<script src>` in Index.html, a `require()` in Code.gs. Both missing-browser paths exercised for real — no `node_modules` exits 1 naming `npm install`; an empty `PLAYWRIGHT_BROWSERS_PATH` exits 1 naming `npx playwright install chromium`. Everything present exits 0. **A skipped run is never reported as a pass.** TIER-4 CANARY: chromium-1234 launches on this machine.

## [2026-07-27 07:50] D2 | 5 of 6 criteria met. The runner builds the document the way Apps Script does — a fragment wrapped in its own head/body with the meta tags injected, no charset meta (that would be a tag doGet never sends). It loads `test/smoke.js` into the page and reads its result; the checks are not reimplemented. Phone viewport 390x844 with isMobile, 17 passed / 0 failed / 2 skipped = 19, matching the 19 `ok(` sites in the file. Verified error paths: a failing check exits 1 and names it; `pass: 0` exits 1; no checks in the file at all exits 1.

## [2026-07-27 07:50] D2 | CHECKER found two design faults in my own first attempt. (1) The runner injected doGet's own meta list and then compared the DOM against it — self-fulfilling, and it left D4 with nothing to compare, since the harness had no list of its own. A fourth tag added to `doGet` passed happily. The harness now declares `META_TAGS` in full, injects that, and cross-checks it against what doGet sends; both drift cases now exit 1 naming which side is missing what. (2) A check deleted from `smoke.js` still passes, because the expected count is derived from the same file — the derived count catches a check that exists but does not run, not one that was removed. Recorded rather than papered over; git review is the guard against deletion.

## [2026-07-27 07:50] D2 | FINDING F3 (fixed): `smoke.js:78` counted `ok('no block running, ring not checked', true, ...)` — literally `true`, a pass whatever the page did, and counted in `pass`. Now reported as two named skips. `pass` counts only checks that could have failed, and `pass + fail + skipped` equals the number of `ok(` sites, so a check added inside an unreached branch cannot silently go unrun. FINDING F4 (PARKED, escalated): no check in `smoke.js` is viewport-sensitive — with no meta tags at all the page lays out at 980px and all 17 still pass — and Blink re-lays-out on a dynamically appended viewport meta, so "after" is indistinguishable from "before". D2's sixth criterion cannot be demonstrated as written. Built the layout-width control instead and left the criterion unmet. Direct evidence that the phone paste is not superseded.

## [2026-07-27 08:05] D3 | DONE. Second viewport added (desktop 1280x800, isMobile false) alongside the phone (390x844, isMobile). Each is labelled with its dimensions and every problem is prefixed with the viewport it came from. Both green: 17 passed / 0 failed / 2 skipped each. CHECKER, both error criteria exercised against fixture copies in scratchpad and never against the repo's own files: a `@media (max-width: 500px)` rule narrowing one cell fails "every cell is the same width (40 / 184)" at the phone only, passes desktop, exits 1 and names phone; a `smoke.js` fixture returning `pass: 0` below 500px exits 1 naming phone while desktop reports its full 17. First attempt at the CSS fixture matched nothing — the grid cells are divs with `role="button"`, not `<button>` elements, because they contain the note `<input>` a button would strip.
