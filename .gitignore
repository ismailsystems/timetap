node_modules/
.DS_Store
_site/

# generated media for the proof video — several MB of mp4, webm, mp3 and png
# that git would keep forever. The recipe is committed instead: beats.json,
# serve-proof.mjs and test-results.html regenerate it from scratch.
factory/proof/out/

# holds your script id and is machine-specific; see SETUP.md section 10
.clasp.json

# Xcode user state for the Path 2 iOS client
ios/**/xcuserdata/
ios/**/*.xcuserstate
ios/TimeTap.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/

# Local API URL + token for device testing — never commit
ios/.secrets/

# Generated from ios/.secrets/GOOGLE_IOS_CLIENT_ID — never commit the real client id
ios/TimeTap/Config/Google.xcconfig
